devidx=0

for _dev in /sys/class/ieee80211/*; do
    [ -e "$_dev" ] || continue
    dev="${_dev##*/}"
    uci set wireless.@wifi-device[$devidx].disabled="0"
    devidx=$(($devidx + 1))
done

exit 0

